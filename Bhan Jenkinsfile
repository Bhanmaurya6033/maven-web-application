node{
  
	properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '', numToKeepStr: '5')), pipelineTriggers([pollSCM('* * * * *')])])
	def mavenHome = tool name: 'maven3.9.8'

	stage('CheckOutCode'){
	git branch: 'development', credentialsId: '1b0f2771-508b-494d-aff3-0d50fbd833de', url: 'https://github.com/Bhanmaurya6033/maven-web-application.git'
	}
	
	stage('Build'){
	sh "${mavenHome}/bin/mvn clean package"
	}
	/*
	stage('ExecuteSonarQubeReport'){
	sh "${mavenHome}/bin/mvn clean sonar:sonar"
}
	
	stage('UploadArtifactsInotNexus'){
	sh "${mavenHome}/bin/mvn clean deploy"
}
    stage('DeployAppIntoTomcatServer'){
    sshagent(['48ea6c5c-40ef-4b81-8ba2-cfa5abed7bcd']) {
    sh "scp -o StrictHostKeyChecking=no target/maven-web-application.war  ec2-user@172.31.94.175:/opt/apache-tomcat-9.0.102/webapps/"
}
}
*/
}
